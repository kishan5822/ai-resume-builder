# Server Configuration
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# File Upload Settings (Optional - defaults work fine)
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# LaTeX Compiler (Optional - auto-detected)
# TECTONIC_PATH=/usr/local/bin/tectonic

# Note: API keys are configured in the UI Settings panel
# Users enter their own OpenRouter or Gemini API keys in the browser
